const peremen = "переменная" //тип переменной conts(константа) неизменяема
let peremenlet = "переменная" //тип переменной let изменяемя
console.log("текст") // вывод текста в консоль
// конструкция if/else
const ger = "банан" // переменная для конструкции
if(ger == "банан") //если переменная ger  == "банан" выполняется условие(если)
{
console.log("переменная = банан ") //проверяем равна ли переменная слову банан
}
else if(ger == "клубника")
{
console.log("переменная = клубника") // проверяем равна ли перемменая слову клубника(иначе если)
}
else
{
console.log("переменная не равна и не клубнике , и не банану") 
}
